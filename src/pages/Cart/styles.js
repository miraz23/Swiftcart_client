import styled from 'styled-components';

const Wrapper = styled.main`
  .empty {
    text-align: center;
    h2 {
      margin: 1rem;
      text-transform: none;
    }
  }
`;

export default Wrapper;
